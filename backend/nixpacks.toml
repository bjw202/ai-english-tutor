[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install ."]

[start]
cmd = "PYTHONPATH=/app/src uvicorn tutor.main:app --host 0.0.0.0 --port $PORT --workers 1"
